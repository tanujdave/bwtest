const defaultOption = {
    container: 'app',
    shapeList: [    // set list of all shape
        'circle',
        'square',
        'rectangle',
        'eclipse'
    ],
    unit: 'm'   // set unit as cm, ft, m
};

export default defaultOption;